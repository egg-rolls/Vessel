# 重构总结：删除 readline REPL + 拆分 cli.ts (#16)

## 目标
1. 删除 readline 版本的 REPL，只保留 Ink 版本（React 组件式 UI）
2. 拆分 cli.ts，提取 Bootstrap 模块和 PluginRegistry（#16 issue）

## 变更清单

### 新增文件
- `src/plugin-registry.ts` - 插件注册表
- `src/bootstrap.ts` - 应用引导模块
- `src/headless-runner.ts` - Headless 运行器
- `packages/tui/src/error-classifier.ts` - 错误分类器

### 修改文件
- `src/cli.ts` - 重构为入口分发器（458行 → ~120行）
- `packages/tui/src/index.ts` - 更新导出
- `packages/tui/src/repl/ink-repl.tsx` - 增强非 TTY 支持
- `packages/tui/__tests__/error-classify.test.ts` - 更新测试引用

### 删除文件
- `packages/tui/src/repl/repl.ts` - readline REPL 实现

## 测试结果
- 152 pass, 0 fail
- 所有测试通过

## 解决的问题
- Closes #16
