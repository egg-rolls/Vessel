feat: P0 fix + P1 provider简化 + P2 plugin激活 + ADR-019 camelCase迁移

P0（修复 bugs/违规项）:
- 修复 setup-wizard 中的 sync-in-async（Bun 文件 API + fs/promises）
- 彻底移除 PROVIDER_PRESETS——Vessel 无状态，不保留厂商 URL/模型列表

P1（Provider 简化）:
- 全部 6 个 provider 插件从 ~140 行 LLMProvider 类缩减为 ~15 行薄工厂
- 删除重复的插件内 AnthropicProvider（统一用核心版）
- Cohere 改用 OpenAI 兼容端点
- 核心 OpenAICompatibleProvider：修 Ollama 空 api_key（有条件跳 Authorization）

P2（激活未使用的插件）:
- memory-auto, guardrail-pii, redact-secrets, tool-policy, mcp-client 进默认加载
- +11 安装/验证测试

ADR-019（camelCase 命名迁移）:
- config 层全 camelCase；loader 加 snake→camel 自动映射
- 核心 Limits/Stats/Termination 改 camelCase
- biome.json：关闭 useNamingConvention + useLiteralKeys（ADR-019 已录规范）
- 130 测试过；CI 全绿；核心 provider 接口不受影响